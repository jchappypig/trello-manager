<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2><%= @casein_page_title %>
  <small>
  Points:
    <% @points.each do |label, point| %>
        <%= label %> <span class="badge"><%= point %></span>
    <% end %>
  </small>
  <%= link_to 'Export to CSV', export_casein_cards_path(format: :csv, ids: @cards['Total'].map(&:id)), target: '_blank', class: 'btn btn-primary pull-right' %>
</h2>

<%= render :partial => 'casein/cards/table', :locals => { :cards => @cards } %>

<%= content_for :sidebar do %>

<% end %>